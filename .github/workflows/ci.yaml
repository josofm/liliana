name: CI

on: [ push ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: Image
        run: make image-dev

      - name: Unit Test
        run: make unit

      - name: Integration Test
        run: make integration