app:
  name: 'liliana'
  version: '1.0.0'

http:
  port: '8080'

logger:
  log_level: 'debug'
  rollbar_env: 'liliana'

jwt:
  secret_key: 'your-super-secret-jwt-key-change-in-production'
  access_expiry: '15m'
  refresh_expiry: '168h'  # 7 dias

database:
  url: ''
